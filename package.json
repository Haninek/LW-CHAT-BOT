{
  "name": "lw-chat-bot-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "cd web && npm install && npm run build && cd .. && mkdir -p server/static && cp -r web/dist/* server/static/ 2>/dev/null || echo 'No dist files'",
    "start": "cd server && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
  },
  "engines": {
    "node": ">=18"
  }
}
